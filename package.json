{
  "name": "badminton-booking",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && rm -rf backend/public && cp -r frontend/dist backend/public",
    "start": "node backend/server.js"
  }
}
